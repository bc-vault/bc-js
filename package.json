{
  "name": "bc-vault-js",
  "version": "1.0.21",
  "description": "Javascript library for interfacing with the BC VAULT daemon via HTTP.",
  "main": "build/main/index.js",
  "typings": "build/main/index.d.ts",
  "module": "build/module/index.js",
  "repository": "https://github.com/bc-vault/bc-js.git",
  "license": "MIT",
  "keywords": [],
  "scripts": {
    "info": "npm-scripts-info",
    "browserify:ie": "browserify build/ie/lib/bcapi.js --standalone _bcvault -o build/bc_js_ie.js",
    "browserify:noie": "browserify build/main/lib/bcapi.js --standalone _bcvault -o build/bc_js_noie.js",
    "build": "run-s clean && run-p build:* && run-p browserify:*",
    "build:main": "tsc -p tsconfig.json",
    "build:module": "tsc -p tsconfig.module.json",
    "build:ie": "tsc -p tsconfig.iecompat.json",
    "test": "run-s build test:*",
    "test:lint": "tslint --project . ",
    "test:unit": "nyc ava",
    "doc": "run-s doc:html && opn build/docs/index.html",
    "doc:html": "typedoc --mode file --excludeNotExported --excludePrivate --gaID UA-118197678-1 --name bc-js --out build/docs",
    "doc:json": "typedoc --mode file --excludeNotExported --excludePrivate --gaID UA-118197678-1 --name bc-js --json build/docs/typedoc.json",
    "doc:publish": "gh-pages -m \"[ci skip] Updates\" -d build/docs",
    "version": "standard-version",
    "reset": "git clean -dfx && git reset --hard && npm i",
    "clean": "trash build test"
  },
  "scripts-info": {
    "info": "Display information about the package scripts",
    "build": "Clean and rebuild the project",
    "test": "Lint and unit test the project",
    "doc": "Generate HTML API documentation and open it in a browser",
    "doc:json": "Generate API documentation in typedoc JSON format",
    "version": "Bump package.json version, update CHANGELOG.md, tag release"
  },
  "engines": {
    "node": ">=8.9"
  },
  "dependencies": {
    "ava": "^1.3.1",
    "axios": "^0.18.0",
    "babel-polyfill": "^6.26.0",
    "bluebird": "^3.5.3",
    "es6-promise": "^4.2.6",
    "npm-run-all": "^4.1.5",
    "opn-cli": "^4.0.0",
    "sha3": "^2.0.0",
    "trash-cli": "^1.4.0"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.26",
    "prettier": "^1.16.4",
    "standard-version": "^4.4.0",
    "tslint": "^5.13.1",
    "tslint-config-prettier": "^1.18.0",
    "tslint-immutable": "^4.9.1",
    "typedoc": "^0.14.2",
    "typescript": "^2.9.1",
    "uglifyify": "^5.0.1"
  },
  "ava": {
    "failFast": true,
    "files": [
      "build/main/**/*.spec.js"
    ],
    "failWithoutAssertions": true,
    "sources": [
      "build/main/**/*.js"
    ]
  },
  "prettier": {
    "singleQuote": true
  },
  "nyc": {
    "exclude": [
      "**/*.spec.js"
    ]
  }
}
